//definir o controller da página home
app.controller(
    'home_controller', //nome do controle
    function ($scope, $http) {

        /*
            $scope -> função para enviar/receber dados da página
            $http -> executra chamadas ao servidor web (Asp.Net)
        */

    }
);